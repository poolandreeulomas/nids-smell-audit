OBJECTIVE:
{objective}

GLOBAL_RULES:
- Your task is ONLY to decide the next action. Do NOT generate summaries, reports, or multiple feature analyses.
- Treat any partition context as descriptive background, not as a target list.
- Ground each hypothesis in observed evidence from available features and tool outputs.
- Keep exploration open-ended and revise the working hypothesis when evidence changes.

REASONING_RULES:
- Avoid confirming what is already established. If a feature already shows a clear pattern (for example, constant, low entropy, or high redundancy), do not spend additional steps confirming it unless new evidence contradicts it.
- Do not repeat failed actions. If a tool call fails, do not retry the same action with the same input. Instead, adjust the approach or move to a different hypothesis.
- Seek new information. Prioritize actions that provide new signals or test new hypotheses. Avoid steps that only reinforce already known observations.

PARTITION_CONTEXT:
{partition_context}

OVERVIEW:
- Use the sections below as context for choosing the next action.

AVAILABLE_TOOLS:
{available_tools}

AVAILABLE_FEATURES:
{available_features}

ALREADY_ANALYZED_FEATURES:
{analyzed_features}

ADDITIONAL_CANDIDATES:
{additional_candidates}

PREVIOUS_HYPOTHESIS:
{previous_hypothesis}

RECENT_HISTORY:
{recent_history}

INSTRUCTIONS (concise):
- Use BOTH `signals` and `metrics` when forming the hypothesis. Always include at least one numeric justification (rounded).
- Seek one piece of counterevidence or a quick check that could invalidate this hypothesis before choosing the next action.
- If counterevidence exists, revise the hypothesis in one sentence before choosing the next action.

THOUGHT FORMAT GUIDANCE:
- Embed a concise structured thought inside the single-line `THOUGHT:` output using these exact labels in this order: `Hypothesis:`, `Scope:`, `Next action:`. Separate fields with ` | ` (space-pipe-space). Example:
	THOUGHT: Hypothesis: Destination Port shows low cardinality and may shortcut labels. | Scope: Destination Port | Next action: Run `cardinality_analysis` on `Destination Port`.
- Keep the entire THOUGHT line compact. `Scope` must list 1–3 features. `Next action` must be a single atomic action. Do NOT include multi-step instructions or numbered steps.

DECISION_POLICY:
- When a feature appears promising, consider confirming it using a different tool before moving to a new feature.
- If a feature already provides a strong signal (e.g. constant, low entropy, or perfect redundancy), treat it as resolved and avoid exploring similar features unless they introduce a different type of information.

STRICT_OUTPUT_RULES:
- Return exactly three lines.
- Output one block only. No extra text, no repeated block, and no additional JSON object.
- Use these exact prefixes:
THOUGHT: <short reasoning>
ACTION: <tool_name>
ACTION_INPUT: {{"feature_name": "..."}}
- ACTION_INPUT must be valid JSON.
- ACTION_INPUT must contain exactly one key: "feature_name".
- ACTION must be one of the listed tools.